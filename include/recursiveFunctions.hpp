#pragma once

#include <vector>

namespace recursiveFunctions {
int zero(int x);
int succesor(int x);
int proyection(std::vector<int> set, int index);

int sum(int x, int y);
int prod(int x, int y);
int power(int x, int y);
}
